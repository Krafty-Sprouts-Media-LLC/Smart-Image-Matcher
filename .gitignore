# OS / IDE
.DS_Store
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Logs / temp
*.log
*.tmp
*.bak
*.swp
*~

# Node
node_modules/
package-lock.json

# Composer vendor (dev deps — not committed, but SHIPPED in the wp.org zip)
# Run `composer install --no-dev` before building the zip.
# The vendor/ folder IS required in production (Action Scheduler lives there).
# Add to .git/info/exclude locally if you don't want it tracked.
# vendor/

# Built JS assets (generated by @wordpress/scripts — not committed)
admin/js/build/

# WordPress
wp-config.php
.htaccess
uploads/
cache/

# -------------------------------------------------------
# wp.org zip exclusions
# These folders are never included in the published build.
# The build script (composer run build-zip or equivalent)
# must also exclude them explicitly.
# -------------------------------------------------------

# Legacy reference — read-only; deleted at v3.0.0 release
.legacy/

# Dev/tooling folders
.agent-skills/
.cursor/
.wp-ai/
.kiro/
docs/audit/
tests/
