/**
 * trigger.js — tiny entry point loaded on every post-edit screen.
 *
 * Lazy-loads modal.js on first button click so the heavy code
 * does not block page load.
 *
 * @package SmartImageMatcher
 * @since   3.0.0
 */

// TODO: Phase 2 — implement lazy load of modal.js.
